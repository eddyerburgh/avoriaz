// @flow

/* eslint-disable import/no-unresolved,import/extensions,eslint-disable-line,no-unused-vars */

// Importing these types declares them, so they are available globally

import type VNode from 'vue/src/core/vdom/vnode';
import type Component from 'vue/flow/component';
import type GlobalAPI from 'vue/flow/global-api';
